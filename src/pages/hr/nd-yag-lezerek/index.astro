---
import CategoryLayout from '../../../layouts/CategoryLayout.astro';
import { getCollection } from 'astro:content';

const category = {
  name: 'Q-Switched ND:YAG laserski uredaji',
  nameShort: 'ND:YAG laseri',
  slug: 'nd-yag-lezerek',
  description: 'Laseri za uklanjanje tetovaza i pigmentnih mrlja.',
  heroDescription: 'Uklanjanje tetovaza, carbon peeling, tretman pigmentnih mrlja. Profesionalna Q-switched tehnologija, minimalna nelagoda.',
  badge: 'Premium tehnologija',
  color: 'purple',
};

const products = await getCollection('products', ({ data }) => {
  return data.categorySlug === 'nd-yag-lezerek';
});
---

<CategoryLayout category={category} products={products} />
