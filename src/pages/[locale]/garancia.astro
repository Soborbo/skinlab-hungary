---
import WarrantyPage from '../../components/pages/WarrantyPage.astro';
import { locales, defaultLocale } from '../../i18n/ui';

export function getStaticPaths() {
  return locales
    .filter((l) => l !== defaultLocale)
    .map((locale) => ({ params: { locale } }));
}
---

<WarrantyPage />
