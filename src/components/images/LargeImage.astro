---
/**
 * LargeImage - LARGE pattern (60vw)
 * Use: Split 60/40 layouts where image takes the larger side
 * Pattern: 60vw desktop, 100vw mobile
 */
import OptimizedPicture from './OptimizedPicture.astro';
import type { ImageMetadata } from 'astro';

interface Props {
  src: ImageMetadata | string;
  alt: string;
  class?: string;
  priority?: boolean;
}

const { src, alt, class: className, priority = false } = Astro.props;

const widths = [320, 480, 640, 960, 1280, 1600, 1920];
const sizes = '(min-width: 1024px) 60vw, 100vw';
---

<OptimizedPicture
  src={src}
  alt={alt}
  widths={widths}
  sizes={sizes}
  class={className}
  loading={priority ? 'eager' : 'lazy'}
  fetchpriority={priority ? 'high' : undefined}
  decoding={priority ? undefined : 'async'}
/>
